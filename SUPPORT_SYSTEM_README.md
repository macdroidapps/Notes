# 🤖 AI Ассистент Поддержки - Реализация

## Обзор

Реализован мини-сервис AI-ассистента для поддержки пользователей приложения заметок. Система использует RAG (Retrieval-Augmented Generation) подход для ответов на вопросы пользователей на основе базы знаний FAQ.

## ✨ Основные возможности

### 1. **AI-ассистент с RAG подходом**
- Поиск релевантных FAQ по запросу пользователя
- Ранжирование результатов по релевантности (TF-IDF-подобный подход)
- Формирование контекстного ответа на основе найденных FAQ
- Fallback ответ когда точного совпадения не найдено

### 2. **База знаний FAQ**
- 13 предзаполненных FAQ по основным темам:
  - ✅ Авторизация
  - 🔄 Синхронизация
  - 📝 Работа с заметками
  - 🏷️ Категории
  - 🎨 Интерфейс
  - ⚡ Производительность
  - 📞 Контакты поддержки

### 3. **Система тикетов**
- Автоматическое создание тикета при первом вопросе
- Сохранение истории диалога
- Определение категории по содержанию вопроса
- Отслеживание статуса (Открыт, В работе, Решён, Закрыт)

### 4. **Удобный UI**
- Чат-интерфейс с AI ассистентом
- Визуальное разделение сообщений пользователя и ассистента
- Индикатор загрузки при обработке запроса
- Возможность очистить историю чата
- Кнопка доступа к поддержке на главном экране (иконка "ℹ️")

## 🏗️ Архитектура

### Clean Architecture с тремя слоями:

```
features/support/
├── data/                           # Data Layer
│   ├── local/
│   │   ├── FAQLocalDataSource.kt
│   │   ├── SupportTicketLocalDataSource.kt
│   │   └── TicketMessageLocalDataSource.kt
│   └── repository/
│       ├── FAQRepositoryImpl.kt
│       └── SupportRepositoryImpl.kt
│
├── domain/                         # Domain Layer
│   ├── model/
│   │   ├── FAQ.kt
│   │   ├── SupportTicket.kt
│   │   ├── TicketMessage.kt
│   │   └── AIAnswer.kt
│   ├── repository/
│   │   ├── FAQRepository.kt
│   │   └── SupportRepository.kt
│   ├── ai/
│   │   └── SupportAIAssistant.kt  # RAG компонент
│   └── usecase/
│       ├── GetAIAnswerUseCase.kt
│       ├── CreateTicketUseCase.kt
│       └── InitializeFAQUseCase.kt
│
└── presentation/                   # Presentation Layer
    ├── viewmodel/
    │   └── SupportChatViewModel.kt
    └── screen/
        └── SupportChatScreen.kt
```

## 🔍 RAG Компонент

### Как работает SupportAIAssistant:

1. **Retrieval (Поиск)**
   ```kotlin
   // Поиск релевантных FAQ в базе данных
   val relevantFAQs = searchRelevantFAQ(question)
   ```

2. **Ранжирование**
   ```kotlin
   // Вычисление скора релевантности:
   // - Совпадение токенов запроса и FAQ
   // - Бонус за популярность (view_count, helpful_count)
   // - Повышенный вес для совпадений в вопросе
   val score = calculateRelevanceScore(query, faq)
   ```

3. **Generation (Генерация)**
   ```kotlin
   // Формирование ответа на основе найденных FAQ
   val answer = generateAnswerFromFAQs(question, relevantFAQs, ticket)
   ```

4. **Confidence (Уверенность)**
   ```kotlin
   // Оценка качества ответа от 0 до 1
   val confidence = calculateConfidence(query, relevantFAQs)
   ```

## 💾 База данных

### SQLDelight схемы:

#### FAQ.sq
```sql
CREATE TABLE FAQ (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    question TEXT NOT NULL,
    answer TEXT NOT NULL,
    category TEXT NOT NULL,
    keywords TEXT NOT NULL,
    view_count INTEGER NOT NULL DEFAULT 0,
    helpful_count INTEGER NOT NULL DEFAULT 0,
    created_at INTEGER NOT NULL
);
```

#### SupportTicket.sq
```sql
CREATE TABLE SupportTicket (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_name TEXT NOT NULL,
    user_email TEXT NOT NULL,
    title TEXT NOT NULL,
    description TEXT NOT NULL,
    status TEXT NOT NULL,
    priority TEXT NOT NULL,
    category TEXT NOT NULL,
    created_at INTEGER NOT NULL,
    updated_at INTEGER NOT NULL,
    resolved_at INTEGER
);
```

#### TicketMessage.sq
```sql
CREATE TABLE TicketMessage (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    ticket_id INTEGER NOT NULL,
    sender_type TEXT NOT NULL,  -- USER, ASSISTANT, SUPPORT
    content TEXT NOT NULL,
    created_at INTEGER NOT NULL,
    FOREIGN KEY (ticket_id) REFERENCES SupportTicket(id)
);
```

## 🚀 Как использовать

### 1. Запуск приложения

```bash
# Android
./gradlew :composeApp:installDebug

# Desktop
./gradlew :composeApp:runDebugExecutableDesktop

# Web
./gradlew :composeApp:jsBrowserDevelopmentRun
```

### 2. Доступ к поддержке

1. На главном экране (список категорий) нажмите на иконку **ℹ️** в правом верхнем углу
2. Откроется чат с AI-ассистентом
3. Задайте вопрос, например:
   - "Почему не работает авторизация?"
   - "Как синхронизировать заметки?"
   - "Не могу создать категорию"

### 3. Взаимодействие с ассистентом

Ассистент:
- Автоматически ищет ответ в базе знаний
- Предлагает связанные FAQ
- Сохраняет историю диалога
- Создаёт тикет для отслеживания

## 📊 Примеры работы

### Пример 1: Успешный поиск ответа

**Вопрос:** "Не синхронизируются заметки"

**Ответ ассистента:**
```
✅ Нашёл ответ в базе знаний:

Для решения проблемы синхронизации:

1. 📱 Убедитесь, что вы вошли под одним аккаунтом на всех устройствах
2. ⚙️ Проверьте, включена ли синхронизация в настройках
3. 📶 Проверьте наличие интернет-соединения
4. 🔄 Попробуйте принудительно запустить синхронизацию
5. 📴 Попробуйте выйти и войти снова

📚 Также может быть полезно:
2. Почему не работает авторизация?

💡 Помог ли вам этот ответ? Пожалуйста, отметьте полезность.
```

### Пример 2: Ответ не найден

**Вопрос:** "Как экспортировать в PDF?"

**Ответ ассистента:**
```
🤔 К сожалению, я не нашёл точного ответа в базе знаний.

Но вот что я могу предложить:
• Попробуйте переформулировать вопрос
• Опишите проблему более детально
• Укажите, когда проблема возникла и какие шаги вы уже предприняли

📞 Ваш тикет передан специалистам поддержки, они ответят в ближайшее время.
```

## 🎯 Технические детали

### Алгоритм поиска (Simple RAG)

1. **Токенизация**
   - Преобразование в lowercase
   - Удаление спецсимволов
   - Фильтрация коротких слов (< 3 символов)

2. **Scoring**
   ```kotlin
   score = matchingTokens 
         + (viewCount * 0.1) 
         + (helpfulCount * 0.5)
         + (questionMatches * 2.0)
   ```

3. **Нормализация confidence**
   ```kotlin
   confidence = min(topScore / (queryTokenCount * 3.0), 1.0)
   ```

### Dependency Injection (Koin)

```kotlin
val supportModule = module {
    // Data Sources
    single { FAQLocalDataSource(faqQueries = get()) }
    single { SupportTicketLocalDataSource(ticketQueries = get()) }
    single { TicketMessageLocalDataSource(messageQueries = get()) }
    
    // Repositories
    single<FAQRepository> { FAQRepositoryImpl(get()) }
    single<SupportRepository> { SupportRepositoryImpl(get(), get()) }
    
    // AI Assistant
    single { SupportAIAssistant(get()) }
    
    // Use Cases
    factory { GetAIAnswerUseCase(get(), get()) }
    factory { CreateTicketUseCase(get()) }
    factory { InitializeFAQUseCase(get()) }
    
    // ViewModel
    viewModel { SupportChatViewModel(get(), get(), get(), get(), get()) }
}
```

## 🔧 Возможные улучшения

### Краткосрочные (можно добавить быстро):
1. ⭐ Оценка полезности ответов (helpful/not helpful)
2. 📈 Статистика просмотров FAQ
3. 🔍 История поиска пользователя
4. 💾 Экспорт истории чата

### Среднесрочные (требуют больше времени):
1. 🧠 Интеграция с Claude/OpenAI API для более умных ответов
2. 🔤 Улучшенная токенизация (стемминг, лемматизация)
3. 📊 Embeddings для семантического поиска
4. 🌐 Многоязычность

### Долгосрочные (полноценная система):
1. 🔗 MCP интеграция для работы с CRM
2. 👤 Система профилей пользователей
3. 📧 Email уведомления
4. 📱 Push-уведомления о статусе тикета
5. 🤝 Передача сложных тикетов живым операторам

## 📝 Заключение

Реализован полностью рабочий минимальный вариант AI-ассистента поддержки с:
- ✅ RAG подходом для поиска ответов
- ✅ Базой знаний из 13 FAQ
- ✅ Системой тикетов
- ✅ Удобным UI чата
- ✅ Clean Architecture
- ✅ Полной интеграцией в существующее приложение

Система готова к демонстрации и может быть легко расширена дополнительными возможностями.

## 🎓 Обучение

Для понимания кода изучите файлы в порядке:
1. `domain/model/*.kt` - Модели данных
2. `domain/ai/SupportAIAssistant.kt` - RAG алгоритм
3. `domain/usecase/GetAIAnswerUseCase.kt` - Бизнес-логика
4. `presentation/viewmodel/SupportChatViewModel.kt` - Управление состоянием
5. `presentation/screen/SupportChatScreen.kt` - UI

---

**Автор:** AI Assistant  
**Дата:** Январь 2026  
**Версия:** 1.0 (Минимальный вариант А)

